{
  "pendingSchema": "screenreader-structured-v12",
  "shippedSchema": "screenreader-structured-v7",
  "openedAt": "2026-08-24",
  "why": "role_name ended an accessible name at end-of-line instead of at the next object's role, so several links announced in one phrase became one name. Fixing it changes what every capture means, so the shipped weights were fitted to a different function of the same evidence and must be retrained before they can be promoted.",
  "closes": "delete this file in the same commit that promotes weights stamped pendingSchema"
}
