{
  "shippedSchema": "screenreader-structured-v18",
  "pendingSchema": "screenreader-structured-v19",
  "why": "The observation FEATURE CROSS. Ten structured features are float(bool(channel)) and any([]) is False, so a 0 means both 'the page has none' and 'nothing looked'. Measured on the authoritative corpus: 61.7% of empty formChanges, 56.1% of empty postSubmitFields and 65.3% of the formControl sweep are the second, so a head can take a free negative weight on a CAPTURE CONDITION rather than a page fact -- ADR 0015's subject. Masking was refuted (not-working: it cost a real finding) and giving the model `observed` as its own column was declined (the shortcut risk), so this crosses the existing fact with whether it was measured: 'asked and present' and 'asked and absent' are two computed columns, and 'never asked' is the all-zeros row carrying no weight of its own. A ZERO CANNOT VETO, so the conjunction is computed rather than handed over as two features. Corrected the same day: the first version crossed `stateChanges` too, and NO capture carries `observed['stateChanges']` -- the disclosure probe is not gated on probeForms, so an empty channel there means one thing and the capture declines to record a question it never had to ask. Those two columns were constant zero, which is a dead column that looks like coverage. Crossed now: `formChanges` (61.7%) and `postSubmitFields` (56.1%), the two the measurement actually named, both with real `observed` entries. Opened 2026-09-03; closes when the retrain following the in-flight recapture stamps v19 into the weights. It is REVERTED, not merged, if any of four gates fails: scorer:shortcuts (closable vetoes must FALL and no head may gain one on a new column), rules:real-pages (zero new findings on the 86 conformant pages), held-out acceptance (must not regress), corpus:distribution (the new columns must not be constant). GATE 4 WAS NAMED WRONG AND IS CORRECTED: it said `corpus:distribution`, which checks whether an ARRAY FIELD is empty on every record — a computed feature is neither a field nor present in the export, so that gate is structurally blind to a constant column. It is now `scorer:shortcuts`' constant-column report, added 2026-09-03 where every feature is already computed over every record. A gate named for a fault it cannot express is worse than no gate.",
  "openedAt": "2026-09-03",
  "closesWith": "the retrain after the in-flight corpus recapture",
  "revertIf": [
    "scorer:shortcuts — closable vetoes do not fall, or a head gains a veto on a new column",
    "rules:real-pages — any new finding against the 86 conformant real pages",
    "held-out acceptance regresses",
    "scorer:shortcuts' CONSTANT-COLUMN report — either new column reads one value across the whole corpus"
  ],
  "notARefutation": [
    "A head with NO VALID CUT at 0.95, the top of the grid. Three heads sit there already — 3.3.1, 4.1.2:state-change-silent, 4.1.3 — and one more negative crossing 0.95 leaves them no clean threshold at all, whereupon the FALLBACK fires and 3.3.1's own sweep measures it at 31 false positives. That is a head going from clean to unusable on ONE record, and it is a property of choosing the threshold as an extreme order statistic, not of anything v19 changed.",
    "A `development.precision` below 1.000. It is the zero-false-positive constraint restated with the cut chosen from those same out-of-fold scores, so 1.000 is guaranteed whenever calibration SUCCEEDS — and below 1.000 can only mean the fallback fired, i.e. the head has no clean cut anywhere."
  ],
  "howToTellThemApart": "READ THE THRESHOLD SWEEP, which `threshold_sweep` already writes into the report as a whole curve, and which the blocker names. CLAUDE.md's rule decides it: at the next cut down, ONE false positive is a record to go and read; FORTY means the head is genuinely weak and the threshold is doing its job. A head that separates its positives cleanly one grid step lower has not regressed — the cut moved.",
  "ungroundedAndNotToBeChangedHERE": "Replacing the zero-false-positive criterion with a quantile or with conformal risk control is the principled fix and is explicitly OUT OF SCOPE for this migration. CLAUDE.md: it trades 'zero on this corpus' for 'bounded in expectation', which for a tool that ASSERTS conformance failures is a decision about what the product promises, not to be made silently. The library carries nothing on conformal prediction (requested 2026-09-03, ISBN 9780123985378), so it is also not a decision to make from memory.",
  "correctedBeforeTheVerdict_2026_09_05": "THE postSubmit PAIR WAS JUSTIFIED UNDER A DEFINITION THE FEATURE DOES NOT READ. Decided and written here BEFORE the chain reported, so it is a correction on the definitions and not a response to a refusal. TWO DEFINITIONS OF 'asked': `observation-ambiguity.mjs:69` sets ASKED_MARK = {formChanges: 'formProbe', postSubmitFields: 'formProbe'} and counts a channel as asked whenever the form probe RAN; `capture-pure.mjs:633` records `observed.postSubmitFields.asked` only when `activated && interaction.formChanges.length > 0`, i.e. a submit HAPPENED and was re-read -- its own `why` says 'probeForms ran and activated nothing, so there was no submit to re-read after'. `observationOf` exports the CAPTURE's definition and `observation_of` reads it, so the 56.1% that justified this pair measured the mark and the feature reads the capture. MEASURED on the 40 local captures carrying the field: by the mark, 18 asked and 10 asked-and-empty (68.8% of empties never asked); by the capture's own record, 8 asked and ZERO asked-and-empty (100% never asked). They disagree on exactly 10 captures -- probe ran, activated nothing. A fact stated twice, and the audit's comment also cites capture-core.mjs:1926 (gone) and claims the record 'does not carry which probes ran' (false since protocol 9). AND THE COLUMN IS UNREACHABLE, NOT MERELY RARE: `CASES` declares `formState` on ZERO of 1,645 cases, and the only formState anywhere is one real page in the `error` state -- SECURITY.md forbids a `success` state on a stranger's site. Nothing in the corpus can produce a submit that navigates to a fieldless confirmation, which is the only path to asked-and-empty for this channel. THE DECISION, UNCONDITIONAL: the postSubmit pair is WITHDRAWN on the ground that its justification measured the wrong definition. It is not conditional on the gate, because the corpus settles it by CONSTRUCTION -- zero of 1,645 cases declare a formState, so asked-and-empty is unreachable and gate 4 will read constant with certainty. That verdict therefore carries no information and is not the reason -- exactly as the `stateChanges` pair was withdrawn above for a dead column -- and v19 becomes the formChanges pair alone, with all four gates applying to it unchanged. THE ONE REMAINING CONDITION, and it can still revert everything: formChanges' 61.7% came from the SAME audit under the SAME mark, so before that pair is kept it must be RE-DERIVED under the feature's definition on the recaptured corpus. The local 10-of-40 asked-and-empty says it should hold. If it does not materially hold, REVERT THE WHOLE OF v19, no partial. SEPARATELY, `ASKED_MARK` is to be fixed to read `observed.<channel>.asked` with the mark as a pre-protocol-9 fallback -- delete the copy, per the house order; `verify.ts`'s `observationOf` already states that rule.",
  "backlogRowsRaisedByThisCorrection": [
    "A SYNTHETIC case with a SUCCESS formState navigating to a fieldless confirmation. It is the only thing that makes asked-and-empty reachable for postSubmitFields, AND it is the 4.1.3 success-message case the corpus does not have. Synthetic pages carry no SECURITY.md objection -- that rule is about submitting on a stranger's site. Costs one capture of the new cases, so it batches with the next deliberate corpus change under stage B's rule rather than paying for a run of its own.",
    "RETIRE `provision.yml` FROM FLEET USE. The role wins outright: every measured divergence is role-ahead (the Edge SHA256 pin the script lacks entirely, LimitBlankPasswordUse hard-fail vs warn, a NIC-power fallback that preserves Wake-on-LAN). Costs one recapture because `provisionRevision` hashes `provision-nvda-worker.ps1` itself, so it rides the next recapture that is happening anyway. UNTIL THEN THE STAMP GAP IS: 5 of 6 `a11y.worker` modules and all 10 role task files are unhashed, so a real bug in `a11y_nic_power.ps1`'s fallback would never move the cache key. Both paths already call the identical stamping script, so there is no per-path split -- an earlier claim of one was wrong and is corrected here."
  ]
}
