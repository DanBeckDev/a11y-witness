---
---

Nothing here can reach a consumer. The new script and its test are in `packages/lab`, which is
`private: true`; the rest is an Ansible playbook (`packages/control`, also private), a doc, and one
number in CLAUDE.md.

Recorded explicitly rather than left to be inferred from silence.
