---

---

#232: `scripts/**/*.mjs` joins the TypeScript program (`// @ts-check` on every file, JSDoc types added
where errors fell out). Comment/JSDoc-only against every published package -- no behaviour, output, or
API surface change a consumer would observe.
