---

---

CI rebuild (ci.yml). Touches `packages/nvda-worker/src/tests-run-without-a-screen-reader.test.ts` (a
comment fix, no behavior change) and Python tests under `packages/lab`/`packages/scorer`. No published
package's runtime behavior changes; empty per `.changeset/README.md`.
