---
---

A coverage STATUS correction and its regenerated doc. `judge` and `cli` are published, and
`criterion-coverage.ts` builds into `judge`'s `dist` — but nothing about what the tool DOES changes: no
rule, no mapping, no finding. What moves is the recorded claim about how much of 4.1.3 is covered, from
`assessed` to `partial`, which is a correction to a public number rather than a behaviour change.

Recorded as empty rather than a patch bump because a consumer's results are byte-identical either side.
