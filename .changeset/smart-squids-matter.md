---
---

#340: `packages/scorer/python/screenreader_features.py` gained a comment-only edit repointing a stale
citation (a dead `schema-migration.json` key) at the new `docs/schema-migration-history.md`. No behaviour,
feature computation, or shipped output changes -- recorded as empty deliberately, per this repo's own
changeset guidance, rather than leaving "does this affect a consumer" to be inferred from silence.

