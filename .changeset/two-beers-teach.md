---
---

Documentation only — `docs/coverage.md` is generated from `criterion-coverage.ts` and no published package
changes. Recorded explicitly so "needs no release" is a decision rather than a silence.
