{
  "//": "THE ONLY PLACE THE BOARD REPORT TAKES A NUMBER IT CANNOT COMPUTE ITSELF, and every entry names who measured it and when. The report reads GitHub for issues, the milestone and merges; those need no entry here. A gate result and the fleet-hours total cannot be read from GitHub, so they are recorded here VERBATIM by the agent that ran them. An absent or stale entry is printed as 'not reported since <date>' -- never omitted, and never estimated. The rule this enforces is the CEO's: nothing in the report comes from what an agent SAID, it comes from what merged and what gates PRINTED, so `output` must be the gate's own text and not a summary of it.",
  "staleAfterHours": 24,
  "gates": [
    {
      "command": "npm run lab:job -- -e job=promote -e ref=main",
      "reportedBy": "orchestrator",
      "at": "2026-09-06T19:30:00Z",
      "output": "1652 discriminating, 0 blind, 0 contaminated, 0 uncaptured, 0 stale, 5 provisional (not yet proven, not blocking)\nPASS \u2014 all 1652 case(s) discriminate, and the corpus is complete. (5 provisional case(s) still BLIND, not counted either way)\n# conformant records\n  1405 scored, 0 false positive(s)\n  2810 of 2810 record(s) carry ruleEvidence; 2810 carry a census\nRULES: PASS \u2014 all 21 of 21 from rule-ownership.json, against 2810 record(s) in /opt/a11y/runs/screenreader-dataset/screenreader-evidence.jsonl examined and clean\n  PASS \u2014 every RULE-ONLY criterion has fired on a real page, or declares why it cannot. Nothing this project claims rests solely on an untested rule.\n  PASS \u2014 all 84 of 84 from conformant real pages scored against the baseline examined and clean\nPromoted candidate.\n  weights   -> /opt/a11y/packages/scorer/models/screenreader-scorer\n  changeset -> /opt/a11y/.changeset/promote-candidate-0d498ef6.md"
    },
    {
      "command": "npm run lab:job -- -e job=capture-only -e only=<7 families> -e capture_root=arm-ten   (then -e workers=5 -e capture_root=arm-five)\nnpm run lab:job -- -e job=fleet-hours -e captures_dir=/opt/a11y/runs/screenreader-dataset/arm-ten   (then arm-five)",
      "reportedBy": "orchestrator",
      "at": "2026-09-06T22:00:00Z",
      "output": "arm-ten   {\"capturesBilled\": 98, \"medianSeconds\": 46.5, \"q1Seconds\": 42.1, \"q3Seconds\": 53.3, \"iqrSeconds\": 11.2, \"p95Seconds\": 60.2, \"dir\": \"/opt/a11y/runs/screenreader-dataset/arm-ten\"}\narm-five  {\"capturesBilled\": 99, \"medianSeconds\": 45.6, \"q1Seconds\": 42.6, \"q3Seconds\": 52.9, \"iqrSeconds\": 10.3, \"p95Seconds\": 58.1, \"dir\": \"/opt/a11y/runs/screenreader-dataset/arm-five\"}\nCapture complete: 49 captured, 0 failed  (both arms)\ncapture window, journal Capturing -> Capture complete:  ten boxes 525s,  five boxes 998s  = 1.90x"
    },
    {
      "command": "npm run lab:job -- -e job=rules-real-pages -e ref=lead/real-page-outcome-is-stated",
      "reportedBy": "orchestrator",
      "at": "2026-09-06T23:40:00Z",
      "output": "  86 conformant real page(s) scored against the baseline.\n# what this gate read, and what it therefore CAN see\n  the CAPTURES, live, under runs/real-page-corpus. A capture-layer fix is visible\n  here immediately, with no re-export needed.\n  `rules:gate` reads the EXPORT (`ruleEvidence` frozen at export time) and will NOT\n  see the same fix until `npm run lab:job -- -e job=export` runs again.\n  the captures this scored were taken:\n    calibration: 57 capture(s), 2026-08-25T07:11:22.413Z .. 2026-09-06T22:50:54.420Z\n    fixture: 10 capture(s), 2026-09-06T17:31:45.124Z .. 2026-09-06T17:32:27.587Z\n    training: 46 capture(s), 2026-08-25T08:14:07.139Z .. 2026-09-06T22:21:44.992Z\n  *** 304 hour(s) between the oldest and newest, so this compares a MIXED population against one baseline.\n  *** `capture-real-pages` defaults to --role=training, which refreshes 40 of the 99. To refresh every role:  npm run lab:pipeline -- --pipeline=real-pages\n  GONE   3.2.1 on design-system.service.gov.uk/components/checkboxes/\n  1 finding(s) no longer reported. Not a failure \u2014 if that was a false positive, this is the fix landing.\n  Run with --update to accept.\n  1 capture(s) have a census this run does not trust: a real second CDP target existed and none was confirmed to be the page navigated to. The census reads as ABSENT to every rule rather than as this OTHER document's numbers; the transcript is unaffected.\n    suspect census: www.w3.org/WAI/demos/bad/after/survey.html\n           census UNUSABLE: targetMatch=fallback candidates=1 -- landed on https://www.w3.org/WAI/demos/bad/after/survey.php, not the requested https://www.w3.org/WAI/demos/bad/after/survey.html; 89 announcement(s) (the transcript is unaffected)\n           opens: \"same page, link, Skip to accessible demo page\" \"visited, link, graphic, W 3C logo, link, graphic, Web Access\" \"heading, level 1, Accessible Survey Page-Before and After De\"\n  4 NEW finding(s) on pages whose publisher declares them conformant:\n    3.3.2  caselaw.nationalarchives.gov.uk/search?query=\n           census heading=7 link=32 graphic=2 graphicUnnamed=0; 82 announcement(s) | DOM heading=7 link=56 graphic=2\n           opens: \"heading, level 2, This website uses cookies\" \"We use some essential cookies to make this service work.\" \"We'd also like to use analytics cookies so we can understand\"\n    4.1.2  caselaw.nationalarchives.gov.uk/search?query=\n           census heading=7 link=32 graphic=2 graphicUnnamed=0; 82 announcement(s) | DOM heading=7 link=56 graphic=2\n           opens: \"heading, level 2, This website uses cookies\" \"We use some essential cookies to make this service work.\" \"We'd also like to use analytics cookies so we can understand\"\n    2.4.2  www.historicenvironment.scot/visit/all/edinburgh-castle/\n           census heading=1 link=1 graphic=0 graphicUnnamed=0; 7 announcement(s) | DOM heading=5 link=2 graphic=0\n           opens: \"heading, level 2, Manage your cookie preferences\" \"We use cookies to ensure our website functions properly, gat\" \"experience on our site, and to show you personalised adverti\"\n    2.4.2  www.lbhf.gov.uk/council-tax\n           census heading=19 link=45 graphic=4 graphicUnnamed=1; 108 announcement(s) | DOM heading=85 link=4237 graphic=2005\n           opens: \"graphic, logo\" \"visited, link, Cookiebot by Usercentrics - opens in a new wi\" \"tab, selected, Consent\"\n  OF THOSE 4: 0 ASSERTED, 4 REFERRED.\n  NOTHING WAS ASSERTED. Every new finding reaches a user as `cantTell`, so this is referral noise\n  on conformant pages rather than a broken conformance claim. Still worth the investigation\n  below, and not a publish blocker.\n  Read the evidence for each before doing anything else. It is one of three things:\n    - the tool is wrong, and this is the defect class that ran for eleven separate causes;\n    - the PAGE changed, since these are live sites their publishers keep editing;\n    - the finding is right and the publisher's claim is not \u2014 which has happened, twice.\n  Only the third takes `--update`.\n  1 page(s) DECLARED unexaminable, and therefore not in the denominator below:\n    www.w3.org/WAI/demos/bad/after/survey.html\n        why: The site rewrites its own extension: the capture landed on survey.php, not the requested survey.html. It IS the requested page, under a URL `sameDocument` cannot recognise as the same document, so the census is refused.\n        removed when: The same `sameDocument` fix. This page is the cheapest possible test of it: same document, different extension, no redirect chain.\n  FAIL \u2014 4 problem(s) across 85 of 85 from conformant real pages scored against the baseline\n"
    },
    {
      "command": "npm run lab:job -- -e job=rules-real-pages -e ref=main",
      "reportedBy": "orchestrator",
      "at": "2026-09-07T12:26:00Z",
      "output": "86 conformant real page(s) scored against the baseline.\n  the captures this scored were taken:\n    calibration: 46 capture(s), 2026-09-07T11:16:37.643Z .. 2026-09-07T11:52:26.336Z\n    training:    40 capture(s), 2026-09-07T11:58:49.285Z .. 2026-09-07T12:24:06.114Z\n\nGONE   3.2.1 on design-system.service.gov.uk/components/checkboxes/\n\n2 NEW finding(s) on pages whose publisher declares them conformant:\n  1.4.13  caselaw.nationalarchives.gov.uk/search?query=\n  2.4.2   www.historicenvironment.scot/visit/all/edinburgh-castle/\n\nOF THOSE 2: 0 ASSERTED, 2 REFERRED.\nNOTHING WAS ASSERTED. Every new finding reaches a user as `cantTell` ... not a publish blocker.\n\nFAIL \u2014 2 problem(s) across 84 of 85 from conformant real pages scored against the baseline",
      "note": "FAIL by the gate's own rule, and **0 asserted** \u2014 the number the board quotes held. Both new findings are referrals on pages whose URLs moved inside the old 298-hour window, and one of them, Historic Environment Scotland, has never been captured past its consent banner on either URL. Not a publish blocker. (Authored by `orchestrator`, who ran the gate.)"
    }
  ],
  "fleetHours": {
    "status": "not instrumented",
    "note": "No cumulative total has been published. `fleet:hours` is built and measured (orchestrator, lead/fleet-hours) and reports 54.11 worker-hours across every capture on disk -- which spans several runs and protocol versions and is therefore NOT any single run's figure, which is why no edition carries it yet. Until an entry names one finished run, the report prints 'not instrumented' in those words.",
    "//": "A FLEET-HOURS ENTRY MUST NAME THE RUN IT WAS COMPUTED FROM **AND** THAT RUN'S finishedAt. The report REFUSES a total missing either, rather than printing it with a caveat -- a footnote is something a reader skips, and unlike a convention a refusal cannot be satisfied by remembering. `runFinishedAt` was added 2026-09-06 on orchestrator's suggestion after a total named a run still in flight: a NAMED but UNFINISHED run makes the field fillable and FALSE, which is harder to catch than a missing one. Shape (the placeholder values below are deliberately not plausible numbers -- an example that reads like data gets copied): {\"total\": \"<HH h MM m, from the instrument, never typed>\", \"run\": \"<unit + InvocationID + case count + protocol>\", \"runFinishedAt\": \"<ISO 8601, the run's own finish, not when you recorded it>\", \"reportedBy\": \"orchestrator\", \"at\": \"<ISO 8601>\", \"method\": \"sum of each capture's last cumulative diagnostics.atMs -- never wall clock x worker count\"}. Delete the `status` key when adding these. THE METHOD IS NOT NEGOTIABLE AND AN EARLIER VERSION OF IT WAS IMPOSSIBLE: this field read 'sum of per-case startedAt..finishedAt from the run's progress file' until orchestrator measured that no such data exists -- 0 of 1,623 cases carry any time field, because only the transient `current` array holds startedAt and an entry leaves it the moment the case completes. The timings a finished run needs are exactly the ones that file has discarded. Same shape as this repo's waiter polling for a `p.captured` that was never a field. The cost lives in each capture's own diagnostics, where bench-capture has read it all along. WHAT THE INSTRUMENT MEASURES, so an edition can caveat it accurately: capture OCCUPANCY only -- not idle time between dispatches, not provisioning, reboots or power. It refuses rather than printing 0.00 when it billed nothing, and distinguishes 'no JSON found' from 'walked N and billed none'. THE `method` STRING IS TO BE COPIED FROM THE INSTRUMENT, NEVER RETYPED. `fleet:hours --json` is gaining a `method` field for exactly this reason (orchestrator, agreed 2026-09-06): the same fact currently lives in CLAUDE.md's `fleet:hours` row AND here, with nothing comparing them, which is this repo's most-repeated defect arriving in the two files that exist to stop people getting this number wrong. The remedy is the one this repo prefers -- delete a copy rather than pin two -- so once that field exists, paste it from the tool's own output and this string is produced by the thing it describes. Until then the string below is correct and was itself a correction: it read 'sum of per-case startedAt..finishedAt from the run's progress file' until orchestrator measured that no such data exists (0 of 1,623 cases carry any time field)."
  },
  "gates_//": "Shape of a `gates` entry: {\"command\": \"npm run lab:job -- -e job=rules-real-pages\", \"reportedBy\": \"orchestrator\", \"at\": \"<ISO 8601>\", \"output\": \"<the gate's OWN text, verbatim, not a summary>\"}. The newest entry by `at` is the one printed. Older than `staleAfterHours` still prints, marked STALE and saying it describes an earlier state of the corpus -- an absent line and an old line mean different things and must never render the same. A measurement that PRE-REGISTERS its falsifier adds \"verdictStatistic\": the name (or list of names) the run promised would settle it, checked by `npm run verdict:preregistered` against this entry's own recorded output. If the run reported a PROXY instead, add \"proxyFor\": \"<the statistic it stands in for>\" -- allowed, and it publishes the caveat WITH the number instead of leaving it in an issue comment, which is where it lived when #22's median went missing and a hardware recommendation turned on the substitute. No entry declares one yet, so the check reports NOTHING TO CHECK (exit 2) rather than a pass -- an entry that declares nothing must never be counted as one that reported everything.",
  "achievements_//": "SECTION 3 OF THE BOARD DOCUMENT: what the product can now DO or PROVE that it could not before -- never commit counts, which measure activity rather than capability. This is the one section no API can derive, so it is authored, and every entry carries the EVIDENCE that makes it a capability claim rather than an assertion. An empty list prints as 'nothing recorded for this window' with what that means, never as a gap the reader has to notice. Shape: {\"claim\": \"<what the product can now do or prove, in a sentence a non-engineer reads>\", \"evidence\": \"<the command, the artefact or the measurement that shows it>\", \"issue\": <number or null>, \"reportedBy\": \"<agent>\", \"at\": \"<ISO 8601>\"}. AN ENTRY WHOSE `evidence` IS ANOTHER AGENT'S SUMMARY IS NOT EVIDENCE. Point at what printed it. `boardClaim` IS WHAT THE BOARD READS and `claim` is what the engineers do. Sections one to five of the board document carry no repository internals at all -- no file names, no identifiers, no command names -- so a claim written for a colleague cannot appear there. The evidence and the exact references stay in the appendix. Added 2026-09-06 on board feedback: every internal reference in edition 1 told them the product manager was pointing at files instead of managing the product. AND THE EVIDENCE BLOCK OPENS WITH WORDS AND ENDS WITH IDENTIFIERS, IN PARENTHESES. Board feedback on edition 2: the body had been cleaned of internals and the appendix was still LEADING with them, which teaches the reader the habit is only hidden. Plain sentence first, exact reference last, so anyone who wants to check can and nobody else has to read past it.",
  "achievements": [
    {
      "claim": "The page written to prove a rule was recorded, and did not prove it.",
      "evidence": "A rule could previously only be demonstrated on pages we wrote ourselves, which cannot falsify the assumptions behind it. A page exercising it was written and has now been recorded \u2014 and the recording showed the opposite of what was expected. The conformant version was measured correctly and the FAILING version was not, because the same property that makes a page fail this rule also prevents the tool from observing it. The tool now reports 'cannot say' on those pages instead of reporting them as clean, and the underlying fix is scheduled. Superseded issue #2, now tracked as #31. (`focus-panel-undismissable-help` in `packages/lab/src/training/real-page-corpus.mjs`; measured 2026-09-06 by orchestrator on both halves of the pair, same fleet, minutes apart.)",
      "issue": 31,
      "reportedBy": "orchestrator",
      "at": "2026-09-07T07:40:00Z",
      "boardClaim": "A page built to prove a rule showed instead that the rule cannot see that fault."
    },
    {
      "claim": "The corpus survives losing its machine.",
      "evidence": "Several thousand screen-reader recordings, which cannot be re-recorded once a browser updates, existed in exactly one place. A copy now sits off that machine, and it was checked by downloading it back and counting what was inside rather than by trusting that the upload succeeded. 7,694 files, 108.1 MB of evidence in an 8.5 MB archive, zero empty files. Each step of the chain distrusts the one before it: the machine holding the corpus verifies its own archive against what is on its disk, the transfer is counted on arrival, and the upload is verified by retrieval. (`corpus:snapshot` -> `lab:fetch -e artifact=corpus-archive` -> `corpus:release`, orchestrator, 2026-09-06; release tag corpus-2026-09-06_19-31-35; tracked as issue #43.)",
      "issue": 43,
      "reportedBy": "orchestrator",
      "at": "2026-09-06T20:45:00Z",
      "boardClaim": "The recordings this product rests on now exist in more than one place.",
      "affirmed": "Re-verified 2026-09-07 21:2xZ by orchestrator, who ran the backup, by downloading the asset back and counting inside it: `corpus:release --verify=corpus-2026-09-06_19-31-35` reports 7,694 JSON files, exit 0, from an 8,960,587-byte asset in `a11ign/corpus-backups`. The claim is that the corpus survives losing its machine, and it still does. It is NOT a claim that the copy is current: the corpus on the lab now holds 7,941 capture files, so the backup is 247 captures behind and a new snapshot is due after the next corpus run."
    },
    {
      "claim": "Adding a job to `gate` put it in `needs:` and not in the pass/fail loop, so `gate` would have waited for it, read its failure and reported success. `ci-changed.test.ts` failed the author's push and exposed it inside the branch; both lists are now derived from the workflow itself and pinned equal, and the derivation was mutation-checked to refuse.",
      "boardClaim": "The check that guards our main line can now be shown to refuse failing work, and it is no longer possible to extend it in a way that quietly cannot.",
      "evidence": "Every change we ship passes one automated check before it can join the main line, and that check cannot be waived by an administrator. It keeps two lists: what it waits for, and what it can fail for. Someone adding a new job put it in the first and not the second \u2014 so that job would have been waited for, seen to fail, and reported as a pass. **It never reached the main line.** A different check failed the author's own push and the fault it exposed was larger than the one it was looking for; the two commits carrying it existed only inside that branch. We examined every state the main line has ever been in \u2014 eighteen of them \u2014 and none had the gap. The fix makes the two lists read from one source, so they cannot disagree again, **and the guard was made to FAIL against the fault before it was trusted**: four earlier guards in this project shipped green against the very fault they were written for, and that demonstration is the difference. This is a statement about how the check is wired, not a re-run of past decisions \u2014 it does not claim that nothing faulty has ever merged, which is a larger question we have not asked. (`gate` in `.github/workflows/ci.yml`, issue #356 via PR #384, fixed by `worker-capture`; found by `ci-changed.test.ts`; trunk audit `git rev-list --first-parent origin/main -- ci.yml`, 18 states, 0 with the gap, run by `dispatcher` and re-derived by `product-manager`.)",
      "issue": 356,
      "reportedBy": "dispatcher",
      "at": "2026-09-07T22:55:00Z"
    }
  ],
  "capacity": {
    "status": "not instrumented",
    "note": "Engineering capacity waiting for its next assignment is not yet instrumented and will print as those words until a week of data exists. It is derived from LABEL EVENTS, which are timestamped: a `session:*` label and `in-progress` are applied together when a row is briefed, so the gap between one session's `in-progress` being removed and that same session's next `in-progress` being added is the measure.",
    "//": "WHY NOT THE ASSIGNEE, which was the first design: this repository has TWO assignable accounts and NINE sessions -- every agent operates as the same GitHub user. The assignee can record assigned-versus-unassigned and cannot record WHICH session, so a week of it would have answered correctly about the wrong thing. Caught by dispatcher before the first day of data rather than after the first week. A per-session LABEL carries attribution and timing in one mechanism, which also removes the second source of truth the first design needed.",
    "definitionChanges": [
      {
        "at": "2026-09-06T~21:00Z",
        "what": "The worker rule changed from pull-AFTER-report to pull-BEFORE-report: a finishing session now takes its next row and reports both in one message.",
        "effect": "The measured gap becomes structurally small from this point. A series spanning this change will show an improvement that is a DEFINITION CHANGE, not a change in how long anyone waited. Any chart or figure crossing it must mark it.",
        "reportedBy": "dispatcher"
      }
    ]
  },
  "capacity_//": "SECTION 5 PRINTS THIS. An entry with `status: not instrumented` prints those words; a real total must name the window it covers AND carry any definitionChanges that fall inside it -- a measure whose definition moved mid-series is two measures wearing one name, which is this project's most-repeated defect with a clock attached."
}